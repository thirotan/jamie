source 'https://rubygems.org/'

gem 'sinatra'
gem 'sinatra-contrib'

# select of database toolkit
gem 'mysql2-cs-bind'
gem 'mysql2'

# select of template engine
gem 'slim'
#gem "sass"

# select of no database
#gem 'redis'

group :test do
  gem 'rspec'
  gem 'rack-test'
  #gem 'capybara'
  #gem 'factory_girl'
end
